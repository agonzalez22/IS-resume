import pdfreader
from pdfreader import PDFDocument, SimplePDFViewer
import os

def get_file_paths(directory):
    """ get files paths
    args:
    directory: directory where resumes are stored

    return:
    resumes: (lst) list of directory names
    
    """



def read_pdf(pdf)